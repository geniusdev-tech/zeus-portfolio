[build]
buildCommand = "cd backend && go build -o ../zeus-backend ./cmd/server/main.go"

[deploy]
startCommand = "./zeus-backend"
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
