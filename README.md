# zeus.dev

Portfolio de Zeus — IT Professional.

- **Frontend:** React + Vite → Vercel
- **Backend:** Go API → Railway

## Setup rápido

```bash
# Frontend
cd frontend && npm install && npm run dev

# Backend
cd backend && make dev
```

Veja `backend/README.md` para instruções de deploy completas.
