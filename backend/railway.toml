[build]
builder = "nixpacks"
buildCommand = "go build -o zeus-backend ./cmd/server/main.go"

[deploy]
startCommand = "./zeus-backend"
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
